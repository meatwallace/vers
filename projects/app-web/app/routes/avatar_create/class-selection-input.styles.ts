import { css } from '@vers/styled-system/css';

export const container = css({
  display: 'flex',
  flexDirection: 'column',
  gap: '2',
});

export const label = css({
  fontSize: 'sm',
  fontWeight: 'bold',
});

export const classSelector = css({
  display: 'flex',
  gap: '2',
});
